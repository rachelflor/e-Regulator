<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_regulator">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ItemActivity"></activity>
        <activity android:name=".SharewithfriendActivity" />
        <activity
            android:name=".SharewithcommunityActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".HomepageActivity"
            android:label="@string/feedpage_title" />
        <activity
            android:name=".CommunityActivity"
            android:label="@string/forumlabel" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/profillabel" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".AddDeviceActivity" />
        <activity
            android:name=".MainActivity"
            android:label="e-Regulator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>